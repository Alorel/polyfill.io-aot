import {MinMaxStepped} from '../../interfaces/VersionDefs';

//tslint:disable:no-magic-numbers

const e: MinMaxStepped<number> = {
  max: 12,
  min: 3,
  steps: [
    0,
    2,
    4
  ]
};
export = e;
